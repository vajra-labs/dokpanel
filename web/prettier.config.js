/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 75,
  arrowParens: 'avoid',
  bracketSpacing: false,
  bracketSameLine: true,
}

export default config
